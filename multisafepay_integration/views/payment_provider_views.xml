<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form" model="ir.ui.view">
        <field name="name">Multisafe Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position='inside'>
                <group invisible="code != 'multisafe'">
                    <field name="multisafe_website_key" required="code == 'multisafe' and state != 'disabled'"/>
                    <field name="multisafe_secret_key" string="Secret Key" required="code == 'multisafe' and state != 'disabled'" password="True"/>
                </group>
            </group>
        </field>
    </record>

</odoo>
